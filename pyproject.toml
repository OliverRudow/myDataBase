[tool.poetry]
name = "myDataBase"
version = "0.3.0"
description = "A robust Python class based on dataclasses for managing, decomposing, and manipulating SQL data bases and Pandas tables."
authors = ["oliverrudow <oliver.rudow@googlemail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.14"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"